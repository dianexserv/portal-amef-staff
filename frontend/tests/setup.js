// Setup global pentru testele frontend.
// Importul `@testing-library/jest-dom` extinde `expect` cu matchere DOM
// (toBeInTheDocument, toHaveTextContent, etc.) folosite în React Testing Library.
import '@testing-library/jest-dom';
